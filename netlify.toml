[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[functions]
  directory = "../netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
  publish = "dist"
  framework = "#custom"
